services:
  app:
    container_name: graphQLServer
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8000:8000
    depends_on:
      - postgres
    env_file:
      - ./env/.env-prod-postgres
  postgres:
    image: postgres:14
    container_name: graphQLPostgres
    command: 
      - -p 5432
    expose: 
      - 5432
    env_file:
      - ./env/.env-prod-postgres
