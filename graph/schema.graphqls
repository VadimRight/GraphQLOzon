# schema.graphqls
type User {
  id: ID!
  username: String!
  password: String!
}

type Post {
  id: ID!
  text: String!
  authorId: ID!
  author: User!
}

type Comment {
  id: ID!
  comment: String!
  authorId: ID!
  author: User!
}

type UserQuery {
  users: [User!]!
  user(id: ID!): User
}

type PostQuery {
  posts: [Post!]!
  post(id: ID!): Post
}

type CommentQuery {
  comments: [Comment!]!
  comment(id: ID!): Comment
}

type Mutation {
  createUser(username: String!, password: String!): User!
  updateUser(id: ID!, username: String!, password: String!): User!
  deleteUser(id: ID!): User!

  createPost(text: String!, authorId: ID!): Post!
  updatePost(id: ID!, text: String!): Post!
  deletePost(id: ID!): Post!

  createComment(comment: String!, authorId: ID!): Comment!
  updateComment(id: ID!, comment: String!): Comment!
  deleteComment(id: ID!): Comment!
}
